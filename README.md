# Dicerolling-Simulator
A simple Python program that simulates rolling a six-sided dice using the random module. The user presses Enter to roll the dice, and the program generates a random number between 1 and 6, mimicking a real dice roll. This beginner-friendly project helps understand basic Python concepts like functions, user input, and random number generation.
